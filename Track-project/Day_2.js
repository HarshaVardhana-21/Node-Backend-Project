//* Git introduction

// * Git commands
    //? git init
    //? git status
    //? git add .
    //? git commit -m "Initial commit"
    //? git branch -M main

    //? git remote add origin <your-repo-link>

        // where to get repo-link:
        // go to github -> new repository -> create new repository -> copy the link

    //? git push -u origin main || git push

//* Pull changes from main
    //? git pull origin main